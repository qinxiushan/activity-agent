# [CHANGE-TYPE] 简短的标题

> **每次修复/改进后，复制此模板到 `docs/changelog/YYYYMMDD-short-description.md` 并填写。**

---

## 1. 元信息

| 字段 | 值 |
|------|-----|
| **日期** | YYYY-MM-DD |
| **类型** | `bug` / `improvement` / `refactor` / `security` / `perf` |
| **严重度** | `P0 阻断` / `P1 高` / `P2 中` / `P3 低` |
| **修改文件数** | N |
| **触发方式** | 手动触发 / 自动检测 / 用户报告 / 代码审查 |
| **关联 Issue** | #N / 无 |

---

## 2. 问题描述

### 2.1 现象

用户/系统观察到的问题。例如：HTTP 500、数据不一致、phase 守卫失效等。

### 2.2 根因

问题产生的根本原因。需要追溯到代码逻辑层面。

### 2.3 影响范围

- 受影响的功能模块
- 是否影响已有用户数据
- 是否影响生产环境

---

## 3. 解决方案

### 3.1 方案选择

列出考虑的方案（2-3 个），解释最终选择的原因。

### 3.2 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `path/to/file.ts` | `add` / `modify` / `delete` | 做了什么，为什么 |

### 3.3 核心逻辑变化

关键代码段的 before/after。只展示最重要的逻辑变化。

---

## 4. 验证

### 4.1 测试结果

| 测试类型 | 结果 |
|---------|------|
| 烟雾测试 | N/N pass |
| 新增测试 | N pass |
| E2E 测试 | N/N pass |
| 手动验证 | 通过/失败 |

### 4.2 验收标准

- [ ] 标准 1
- [ ] 标准 2
- [ ] 标准 3

---

## 5. 回滚方案

如果需要回滚此变更：

```bash
# 具体命令
```

回滚影响：xxx

---

## 6. 后续改进

- [ ] 可以进一步优化的点
- [ ] 需要跟进的任务
